%h1 Übersicht aller registrierten Transporter

- @transporter.each do |t|
  - if current_user.is_admin?
    %h3 Name:
    = t.name
    %h3 Email:
    = t.email
    %br/
    = link_to "Transporter bearbeiten", edit_transporter_path(t)

  - else
    %h3 Name:
    = t.name
    %h3 Email:
    = t.email
