%h1 Neue Donation erstellen
= bootstrap_form_for [@donator, @donation] do |f|
  #{f.text_field :food}
  #{f.number_field :amount}
  #{f.select :unit, options_for_select(["kg", "Stück", "Liter"])}
  #{f.date_select :expiry_date}
  %li
    = f.submit "Spende einstellen", class: "btn btn-success"

= link_to "Back", donator_donations_path(@donator)
